[package]
name = "aux-dsp-comp-color"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

# Skip wasm-opt — same reason as dsp-eq8 / dsp-comp-clean.
[package.metadata.wasm-pack.profile.release]
wasm-opt = false
