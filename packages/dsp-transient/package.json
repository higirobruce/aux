{
  "name": "@aux/dsp-transient",
  "version": "0.1.0",
  "private": true,
  "description": "Transient designer (attack / sustain shaping) — Rust DSP compiled to WASM.",
  "type": "module",
  "main": "./wasm/transient.js",
  "types": "./wasm/transient.d.ts",
  "files": ["wasm"],
  "exports": {
    ".": {
      "types": "./wasm/transient.d.ts",
      "default": "./wasm/transient.js"
    },
    "./transient_bg.wasm": "./wasm/transient_bg.wasm",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "wasm-pack build --release --target web --out-dir wasm --out-name transient && rm -f wasm/.gitignore wasm/package.json",
    "test:rust": "cargo test --release",
    "clean": "rm -rf wasm target"
  }
}
