{
  "name": "@aux/dsp-mbcomp",
  "version": "0.1.0",
  "private": true,
  "description": "3-band multiband compressor — Rust DSP compiled to WASM.",
  "type": "module",
  "main": "./wasm/mbcomp.js",
  "types": "./wasm/mbcomp.d.ts",
  "files": ["wasm"],
  "exports": {
    ".": {
      "types": "./wasm/mbcomp.d.ts",
      "default": "./wasm/mbcomp.js"
    },
    "./mbcomp_bg.wasm": "./wasm/mbcomp_bg.wasm",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "wasm-pack build --release --target web --out-dir wasm --out-name mbcomp && rm -f wasm/.gitignore wasm/package.json",
    "test:rust": "cargo test --release",
    "clean": "rm -rf wasm target"
  }
}
